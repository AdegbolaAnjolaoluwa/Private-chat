# Private Chat Backend Environment Configuration

# Database connection string for Postgres
# Format: postgres://user:password@localhost:5432/private_chat?sslmode=disable
DB_URL=postgres://postgres:password@localhost:5432/private_chat?sslmode=disable

# Server port
PORT=4000

# JWT secret for token signing (use a strong random string in production)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-super-secret-key-change-this-in-production

# Enable debug logging
DEBUG=false
